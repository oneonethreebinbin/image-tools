# viewmax.top 工具站规划文档

## 目标

把 `viewmax.top` 从单一图片工具页面，升级成一个可持续扩展的在线工具网站。当前核心功能是图片去水印和图片压缩，后续可以继续增加视频链接解析、格式转换、图片裁剪等小工具。

目标不是做很多零散页面，而是围绕一个域名沉淀 SEO 权重，让 Google 能把每个功能识别成独立工具页，同时满足 AdSense 审核和长期投放要求。

## 推荐网站结构

建议保留一个主域名，不需要为每个功能买新域名。

```text
https://www.viewmax.top/
https://www.viewmax.top/image-watermark-remover
https://www.viewmax.top/image-compressor
https://www.viewmax.top/video-link-extractor
```

首页作为工具导航页，单独工具使用独立 URL。

当前项目仍是 `#watermark`、`#compress` 这种 hash 页面，对用户可用，但对 SEO 不如真实路径。下一步建议引入 Vue Router，把每个工具做成独立路由。

## 首页定位

首页应该定位为：

```text
免费在线图片与视频工具箱
```

首页内容建议包含：

- 图片工具入口
- 视频工具入口
- 简短介绍
- 隐私说明
- 常见问题
- 工具分类导航

首页不要只放一个功能，否则以后新增工具时网站定位会变窄。

## 当前图片工具页面

### 图片去水印页面

推荐 URL：

```text
/image-watermark-remover
```

推荐 SEO：

```text
Title:
免费图片去水印工具 - AI 在线去水印 | ViewMax

Description:
免费在线图片去水印工具，支持 AI 修复、智能填充、模糊和裁剪，适合处理照片、商品图和社交媒体图片中的水印。

H1:
免费图片去水印工具
```

页面内容建议：

- 上传图片
- 框选水印
- 选择 AI 修复、智能填充、模糊或裁剪
- 下载结果
- 使用说明
- FAQ

### 图片压缩页面

推荐 URL：

```text
/image-compressor
```

推荐 SEO：

```text
Title:
免费图片压缩工具 - 在线压缩 JPG/PNG/WebP | ViewMax

Description:
免费在线图片压缩工具，支持 JPG、PNG、WebP 图片压缩，可调整质量、输出格式和最大尺寸，快速减小图片体积。

H1:
免费图片压缩工具
```

页面内容建议：

- 上传图片
- 设置压缩质量
- 选择输出格式
- 限制最大尺寸
- 预览压缩前后大小
- 下载压缩图片
- FAQ

## 新增视频链接解析工具

### 推荐定位

建议不要直接叫“万能视频下载器”，因为这类表达容易涉及版权和平台规则，也可能影响 AdSense 审核。

更安全的名称：

```text
视频链接解析工具
```

推荐 URL：

```text
/video-link-extractor
```

推荐 SEO：

```text
Title:
在线视频链接解析工具 - 提取视频信息与封面 | ViewMax

Description:
输入公开视频链接，提取视频标题、封面和可访问的视频信息。请仅解析自己拥有权利或已获得授权的内容。

H1:
在线视频链接解析工具
```

### 合规文案

页面需要放一段明显说明：

```text
请仅解析您拥有版权、获得授权或允许下载的公开视频内容。本站不鼓励也不支持侵犯版权或违反第三方平台服务条款的行为。
```

这对 AdSense 审核很重要。

### 功能设计

前端流程：

```text
输入视频页面链接
点击解析
显示视频标题
显示封面图
显示可用格式或媒体信息
提供复制链接或下载入口
```

后端接口建议：

```text
POST /api/video/extract
```

请求：

```json
{
  "url": "https://example.com/video-page"
}
```

返回：

```json
{
  "title": "视频标题",
  "thumbnail": "https://example.com/cover.jpg",
  "duration": 120,
  "videos": [
    {
      "quality": "720p",
      "format": "mp4",
      "url": "https://example.com/video.mp4"
    }
  ]
}
```

### 技术注意

视频解析通常不建议只放在前端，因为很多网站有：

- CORS 限制
- 动态签名
- 反爬策略
- 页面结构变化
- 需要服务端抓取 HTML

项目里已有 `backend/server.py`，可以在这个后端上扩展视频解析接口。

## SEO 策略

每个工具页都要有独立的：

- URL
- Title
- Description
- H1
- 页面说明文字
- FAQ
- sitemap 记录

推荐 sitemap：

```xml
<url>
  <loc>https://www.viewmax.top/</loc>
</url>
<url>
  <loc>https://www.viewmax.top/image-watermark-remover</loc>
</url>
<url>
  <loc>https://www.viewmax.top/image-compressor</loc>
</url>
<url>
  <loc>https://www.viewmax.top/video-link-extractor</loc>
</url>
```

中英文版本可以使用：

```text
?lang=zh
?lang=en
```

或后续升级为：

```text
/zh/image-compressor
/en/image-compressor
```

如果暂时不想复杂化，继续使用 `?lang=zh` 和 `?lang=en` 即可。

## AdSense 注意事项

AdSense 审核重点包括：

- 网站可以被 Google 抓取
- 内容不是空壳
- 页面有真实功能
- 没有明显版权风险
- 没有诱导点击广告
- 广告位不遮挡工具操作

视频工具尤其要注意版权表达。不要在页面里写：

```text
下载 YouTube 视频
下载 TikTok 视频
破解视频下载
无水印下载所有平台视频
```

建议使用更稳妥的表达：

```text
解析公开视频信息
提取授权内容的视频链接
查看视频封面与媒体信息
```

## 推荐实施顺序

### 第一阶段：整理现有站点

1. 引入 Vue Router。
2. 把当前两个功能拆成真实页面：
   ```text
   /image-watermark-remover
   /image-compressor
   ```
3. 首页改成工具导航页。
4. 更新 `sitemap.xml`。
5. 保留中英文切换。

### 第二阶段：增强 SEO

1. 每个工具页加 FAQ。
2. 每个工具页加独立 meta。
3. 添加结构化数据 `SoftwareApplication` 或 `WebApplication`。
4. 给首页加工具分类。

### 第三阶段：新增视频链接解析

1. 新建 `/video-link-extractor` 页面。
2. 新增后端接口 `/api/video/extract`。
3. 支持基础公开视频解析。
4. 加版权声明。
5. 加入 sitemap。

### 第四阶段：继续扩展工具

可以继续增加：

- 图片格式转换
- 图片裁剪
- 图片尺寸调整
- WebP 转 JPG
- JPG 转 WebP
- 视频封面提取
- 视频转音频

优先做图片类工具，因为和当前站点主题一致，AdSense 风险更低，SEO 相关性也更强。

## 最终建议

短期不要再买新域名。先把 `viewmax.top` 做成一个垂直工具站。

推荐方向：

```text
ViewMax = 在线图片与视频工具箱
```

先把图片工具做扎实，再谨慎增加视频解析。每新增一个功能，都应该是一个独立页面，而不是简单加一个按钮或 Tab。
